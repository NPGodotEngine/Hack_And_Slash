extends Node

# Attachment type
##
# `STOCK` gun stock
# `TRIGGER` gun trigger
# `AMMO` gun ammo
# `BARREL` gun barrel
# `ALT` fun alternative fire
enum AttachmentType {
    STOCK = 1,
    TRIGGER = 2,
    AMMO = 4,
    BARREL = 8,
    ALT = 16
}

